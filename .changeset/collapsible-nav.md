---
"runray": patch
---

The navigation rail collapses to icons and back: the chevron at its foot,
the `[` key, or the palette. Every destination keeps its name for
assistive tech and a tooltip, and the choice persists like the theme.
